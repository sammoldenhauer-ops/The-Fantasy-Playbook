import { ComingSoon } from "@/components/coming-soon";

export default function LeaguePage() {
  return (
    <ComingSoon
      title="League analyzer"
      description="League-wide standings, playoff odds, schedule context, and matchup edges are on the way."
    />
  );
}
